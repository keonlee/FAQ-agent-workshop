# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: faq-agent-workshop
metadata:
  template: faq-agent-workshop@1.0.0

services:
  api:
    project: ./
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
